import { Button } from '@material-ui/core'
import React from 'react'

const FilterDiscount = ({discount,discountHeandler}) => {
  return (
    <Button onClick={(e) => discountHeandler(50)}>ssssssssssssss</Button>
  )
}
export default FilterDiscount